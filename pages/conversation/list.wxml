<scroll-view class='rong-conversations'>
  <Conversation bindtap='gotoChat'  
    bindlongpress="showConversationHandles" 
    wx:for="{{conversationList}}" 
    wx:for-item="conversation" 
    wx:key='{{conversation.targetId}}' 
    data-item='{{conversation}}' 
    conversation='{{conversation}}'>
  </Conversation>
</scroll-view>
